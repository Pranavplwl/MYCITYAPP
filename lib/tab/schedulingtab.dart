import 'package:flutter/material.dart';

class SchedulingTab extends StatelessWidget {
  const SchedulingTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("SCHEDULING TAB"),
    );
  }
}
