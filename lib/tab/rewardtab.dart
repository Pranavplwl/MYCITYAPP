import 'package:flutter/material.dart';

class RewardTab extends StatelessWidget {
  const RewardTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("REWARD TAB"),
    );
  }
}
